# Memory Events: 2026-03

Append-only implementation events. Do not rewrite old entries for content
changes; append a new event instead.

## MEM-2026-03-11-001

- Date: 2026-03-11
- Scope: Reviewed and hardened the `context` prompt-contract feature added for
  GitHub issue #2.
- Decision:
  - Keep `context` aligned with official Qwen3-ASR semantics across offline,
    batch, session, streaming, and CLI surfaces, and treat missing public-surface
    tests/docs as release blockers for feature-quality signoff.
- What worked:
  - Comparing directly against upstream `qwen3_asr.py` and the shipped
    `chat_template.json` made the prompt contract unambiguous.
  - Small public-surface tests for Session and CLI closed the most likely
    regression gaps without touching inference logic.
- What did not work:
  - The initial feature commit updated core code plus README/architecture but
    left decision-history and some public-surface verification incomplete.
- Reuse next time:
  - For upstream-parity features, verify four layers in one slice: implementation,
    public API tests, CLI tests, and decision/docs trail.
- Evidence:
  - `mlx_qwen3_asr/tokenizer.py`
  - `mlx_qwen3_asr/transcribe.py`
  - `mlx_qwen3_asr/session.py`
  - `mlx_qwen3_asr/cli.py`
  - `tests/test_session.py`
  - `tests/test_cli.py`
  - `README.md`
  - `docs/DECISIONS.md`

## MEM-2026-03-20-001

- Date: 2026-03-20
- Scope: Performed a code review of the built-in HTTP transcription server.
- Decision:
  - Treat the current server as not production-ready until auth/resource
    handling, queue semantics, and spec/implementation mismatches are fixed.
- Reuse next time:
  - For HTTP surfaces, review auth order, tenant isolation, backpressure
    atomicity, and whether CLI/spec flags are actually enforced in code.
- Evidence:
  - `mlx_qwen3_asr/server.py`
  - `mlx_qwen3_asr/cli.py`
  - `tests/test_server.py`
  - `docs/server/API-SPEC.md`
  - `mlx_qwen3_asr/session.py`

## MEM-2026-03-20-002

- Date: 2026-03-20
- Scope: Reviewed OpenAI-compatible `/v1/audio/transcriptions` behavior against
  the local OpenAI Python SDK and current OpenAI API schema.
- Decision:
  - Treat non-streaming `json`/`text`/current `verbose_json` parsing as SDK-safe
    for now, but treat queue bypass and missing streaming/timestamp-granularity
    parity as the main blockers for claiming real OpenAI compatibility.
- Reuse next time:
  - For compatibility endpoints, verify two things explicitly: the current SDK's
    concrete cast types and whether the server preserves the same concurrency and
    backpressure model as the native path.
- Evidence:
  - `mlx_qwen3_asr/server.py`
  - `mlx_qwen3_asr/session.py`
  - `tests/test_server.py`
  - local `openai==2.17.0`
  - OpenAI audio transcription OpenAPI schema
