# Implementation Memory - 2026-09

## MEM-2026-09-06-001

- Date: 2026-09-06
- Scope: Issue #16 / PR #18. Server inference failed on MLX 0.31+ with
  `There is no Stream(gpu, 1) in current thread`.
- Decision:
  - Root cause is library-level, not server-level: init-time buffers that are
    not parameters (`SinusoidalPositionEmbedding._pe`,
    `InterleavedMRoPE._inv_freq`) were lazy graphs bound to the constructing
    thread. Materialize them with `mx.eval` in `__init__`.
  - Keep PR #18's dedicated single-worker executor in the server as defence in
    depth, with `try/finally` cleanup and `shutdown(wait=True)` off the loop.
- Reuse next time:
  - Bisect thread-affinity failures op by op on a worker thread; `nn.Linear`
    weights and `mx.load` tensors are portable, only unevaluated graphs are
    not. `mx.eval(model.parameters())` does not reach underscore attributes.
  - Build test inputs inside the worker thread; otherwise `mx.zeros` created
    on the test thread reproduces the bug for the wrong reason.
  - Keep a second venv with the newest MLX; the repo venv (0.30.6) hides
    every thread-affinity bug and the newer `as_strided` error.
- What did not work:
  - `mx.synchronize()` or `mx.set_default_stream(...)` on the worker thread.
  - Evaluating `model.parameters()` alone.
- Evidence:
  - Probe matrix on MLX 0.32.2: load main/infer worker FAIL; load worker A /
    infer worker B FAIL; load+infer same thread OK; after buffer eval all OK.
  - `tests/test_model.py::TestThreadPortability` fails on MLX 0.32.2 without
    the fix and passes with it; passes on 0.30.6 either way.
  - `mlx_qwen3_asr/encoder.py`, `mlx_qwen3_asr/mrope.py`,
    `mlx_qwen3_asr/server.py`

## MEM-2026-09-06-002

- Date: 2026-09-06
- Scope: Issue #15. Chinese SRT cues cut every 10 characters.
- Decision:
  - The forced aligner drops punctuation for every language and emits one
    segment per CJK character, so `max_words=10` cut phrases. Subtitle grouping
    now restores transcript punctuation onto segments (grouping only, the
    `segments` contract is unchanged), measures width in display cells, applies
    the word cap only to space-delimited languages, and prefers clause and
    sentence boundaries.
- Reuse next time:
  - Reproduce subtitle issues with `say -v Meijia` (macOS Mandarin voice) piped
    through ffmpeg to 16 kHz WAV; the aligner needs
    `Qwen/Qwen3-ForcedAligner-0.6B` cached.
  - Building regexes from f-strings with `]` inside a character class silently
    breaks; use `re.escape` for the closer set.
- Evidence:
  - Before: `大家好今天我们继续学` / `习真实义品上一次我们`.
    After: `大家好，今天我们继续学习真实义品。` / `上一次我们讲到三曲空这一段，`.
  - `mlx_qwen3_asr/writers.py`, `tests/test_writers.py`

## MEM-2026-09-06-003

- Date: 2026-09-06
- Scope: Nightly regression red since August.
- Decision:
  - Guard `stft()` against inputs of `<= n_fft // 2` samples. Newer MLX raises
    an opaque `as_strided` error instead of yielding zero frames.
- Reuse next time:
  - When a scheduled workflow goes red, check whether the failure is an MLX
    behaviour change first; the repo venv pins an older MLX than CI installs.
- Evidence:
  - Nightly run 2026-09-06: `test_audio.py::test_raises_for_too_short_audio`.
  - `mlx_qwen3_asr/audio.py`, `tests/test_audio.py`

## MEM-2026-09-06-004

- Date: 2026-09-06
- Scope: PR #17 (`--diarize-device`) merge and follow-ups.
- Decision:
  - Merged as-is, then added an inference-time CPU retry (see Decision 27),
    documented the flag in README, and dropped the AST call-site test that
    would reject a legitimate `**kwargs` splat.
- Reuse next time:
  - For torch backends, guard the model *call*, not `.to()`. Fake pipelines in
    tests should expose a `device` attribute and fail on call for accelerators.
- Evidence:
  - `tests/test_diarization.py::test_infer_speaker_turns_retries_on_cpu_when_accelerator_inference_fails`

## MEM-2026-09-06-005

- Date: 2026-09-06
- Scope: Review findings from five parallel read-only review agents (core,
  pipeline, docs, tests/CI, PR #17).
- Decision:
  - Landed: float16 PE (3.5x faster float16 inference, WER unchanged at 2.33%
    on 100 LibriSpeech samples), LM-head re-tie (-311 MB), bounded mask caches,
    pre-loaded-model tokenizer path, speculative draft-cache alignment, server
    upload streaming + `--max-duration` + resilient worker + 400 on bad
    requests, `transcribe` subcommand, one language-aware joiner (Korean
    spacing), `TranscribeOptions` de-duplication, removal of `supports_kwarg`
    shim and dead modules, mypy gate widened to 15 modules, doc drift fixes and
    archive of dated notes.
  - Also landed after the first review pass: `cli.main()` split into
    parser/validation/mic/file/output helpers; ruff `B` and `SIM` enabled
    (SIM108 and SIM905 ignored on purpose, B008 ignored for FastAPI defaults).
- Reuse next time:
  - Subagents cannot write report files; ask for findings inline in chunks of
    under 12k characters.
  - A `| tail` after pytest hides the exit code; check `$?` separately before
    committing.
  - Verify any perf or dtype change on the real model with the LibriSpeech
    lane and per-sample hypothesis diff, not only unit tests.
- Evidence:
  - Branch `review/2026-09-maintenance`; `scratchpad/wer_baseline_pe32.json`
    vs `wer_pe16.json` (identical hypotheses, RTF 0.171 -> 0.049).
